"""Foundry — Python GAN that bootstraps the wiki-brain training corpus.

See PLAN.md and plans/foundry.md for the active build plan.

Phases (see plans/foundry.md):
    0. Landing page (this repo's index.html)
    1. Foundry scaffold (config + CLI) — current
    2. Inspector + Python builder (re-impl of forge buildSingle)
    3. Discriminator (functional + depth + size + LLM judge)
    4. Generator + GAN refinement loop
    5. Fine-tune target (wiki-brain JSONL → in-browser model)

This module is intentionally empty until Phase 1 lands.
"""

__version__ = "0.0.0"
